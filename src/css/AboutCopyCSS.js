import styled from '@emotion/styled';

const styles = {
  copyContainer: styled.div({
    height: '242px',
    overflowY: 'hidden',
    boxShadow: 'inset 0 -10px 10px -13px black'
  }),

  readMore: styled.p({
    margin: '0',
    fontSize: '14px',
    textDecoration: 'underline rgb(100, 100, 100) dotted',
    cursor: 'pointer'
  })
};

export default styles;